import { View} from "react-native";

// Implement the drawing of a line graph using bezier curves
export default function Graph() {
  return <View style={{backgroundColor: "grey", width: 350, height: 350}}></View>
}